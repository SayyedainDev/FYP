rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Simplified for FYP: Any logged-in user can read and write any document.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}
